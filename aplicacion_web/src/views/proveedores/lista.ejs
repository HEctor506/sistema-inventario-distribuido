<div class="d-flex justify-content-between align-items-center mb-4">
  <h2 class="mb-0">Proveedores</h2>
  <a href="/proveedores/crear" class="btn btn-primary">
    <i data-lucide="plus" class="me-1"></i> Nuevo Proveedor
  </a>
</div>

<% if (typeof success !== 'undefined' && success) { %>
  <div class="alert alert-success alert-dismissible fade show" role="alert"><%= success %>
    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
  </div>
<% } %>
<% if (typeof error !== 'undefined' && error) { %>
  <div class="alert alert-danger alert-dismissible fade show" role="alert"><%= error %>
    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
  </div>
<% } %>

<div class="card shadow-sm">
  <div class="card-body p-0">
    <div class="table-responsive">
      <table class="table table-hover mb-0">
        <thead class="table-light">
          <tr>
            <th>ID</th>
            <th>Nombre</th>
            <th>Contacto</th>
            <th>Teléfono</th>
            <th>Email</th>
            <th>Productos</th>
            <th class="text-end">Acciones</th>
          </tr>
        </thead>
        <tbody>
          <% if (!proveedores || proveedores.length === 0) { %>
            <tr><td colspan="7" class="text-center text-muted py-4">No hay proveedores</td></tr>
          <% } else { %>
            <% proveedores.forEach(pr => { %>
              <tr>
                <td><%= pr.proveedor_id %></td>
                <td><%= pr.nombre %></td>
                <td><%= pr.contacto || '-' %></td>
                <td><%= pr.telefono || '-' %></td>
                <td><%= pr.email || '-' %></td>
                <td><span class="badge bg-secondary"><%= pr.cantidad_productos %></span></td>
                <td class="text-end">
                  <a href="/proveedores/editar/<%= pr.proveedor_id %>" class="btn btn-sm btn-outline-primary me-1"><i data-lucide="pencil"></i></a>
                  <form action="/proveedores/eliminar/<%= pr.proveedor_id %>" method="POST" class="d-inline" onsubmit="return confirm('¿Eliminar este proveedor?');">
                    <button type="submit" class="btn btn-sm btn-outline-danger"><i data-lucide="trash-2"></i></button>
                  </form>
                </td>
              </tr>
            <% }) %>
          <% } %>
        </tbody>
      </table>
    </div>
  </div>
</div>
<script>lucide.createIcons();</script>
